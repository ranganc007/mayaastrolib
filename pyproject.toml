[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mayaastrolib"
version = "0.3.0"
description = "Python library for Traditional and Vedic Astrology (fork of flatangle/flatlib)"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
keywords = ["astrology", "traditional astrology", "vedic astrology", "swiss ephemeris"]
authors = [
    { name = "João Ventura", email = "flatangleweb@gmail.com" },
]
maintainers = [
    { name = "Rangan C.", email = "ranganc@gmail.com" },
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Development Status :: 4 - Beta",
    "Environment :: Other Environment",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "pyswisseph>=2.10.3.2",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-cov",
    "ruff",
    "mypy",
    "skyfield>=1.46",  # tests/golden/generate_fixtures.py — MIT, dev-only
]

[project.urls]
Homepage = "https://github.com/ranganc007/mayaastrolib"
Upstream = "https://github.com/flatangle/flatlib"

[tool.setuptools]
packages = [
    "mayaastrolib",
    "mayaastrolib.dignities",
    "mayaastrolib.ephem",
    "mayaastrolib.predictives",
    "mayaastrolib.protocols",
    "mayaastrolib.tools",
    "flatlib",
    "flatlib.dignities",
    "flatlib.ephem",
    "flatlib.predictives",
    "flatlib.protocols",
    "flatlib.tools",
]

[tool.setuptools.package-data]
mayaastrolib = [
    "resources/README.md",
    "resources/swefiles/*.se1",
    "resources/swefiles/*.cat",
    "resources/swefiles/*.txt",
]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "A", "UP"]
ignore = ["UP031"]  # see docs/RUFF-DEBT.md

[tool.mypy]
python_version = "3.10"
warn_unused_ignores = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--strict-markers"
pythonpath = ["."]

[tool.coverage.run]
source = ["mayaastrolib"]
